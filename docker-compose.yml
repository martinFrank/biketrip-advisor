services:
  backend:
    build: ./backend
    environment:
      - BIKETRIP_OLLAMA_BASE_URL=http://host.docker.internal:11434
    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
